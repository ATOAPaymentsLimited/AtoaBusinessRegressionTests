appId: ${APP_ID}
---
- runFlow: 
    when: 
      true: ${ROLENAME1} 
    commands:
      - assertVisible:
          text: ${output.employeesPage.roleTile}
          containsDescendants: 
            - ${ROLENAME1} 

- runFlow: 
    when: 
      true: ${ROLENAME2} 
    commands:
      - assertVisible:
          text: ${output.employeesPage.roleTile}
          containsDescendants: 
            - ${ROLENAME2} 

- runFlow: 
    when: 
      true: ${ROLENAME3} 
    commands:
      - assertVisible:
          text: ${output.employeesPage.roleTile}
          containsDescendants: 
            - ${ROLENAME3} 

- runFlow: 
    when: 
      true: ${ROLENAME4} 
    commands:
      - assertVisible:
          text: ${output.employeesPage.roleTile}
          containsDescendants: 
            - ${ROLENAME4} 

- runFlow: 
    when: 
      true: ${ROLENAME5} 
    commands:
      - assertVisible:
          text: ${output.employeesPage.roleTile}
          containsDescendants: 
            - ${ROLENAME5} 

- runFlow: 
    when: 
      true: ${ROLENAME6} 
    commands:
      - assertVisible:
          text: ${output.employeesPage.roleTile}
          containsDescendants: 
            - ${ROLENAME6} 

- runFlow: 
    when: 
      true: ${ROLENAME7} 
    commands:
      - assertVisible:
          text: ${output.employeesPage.roleTile}
          containsDescendants: 
            - ${ROLENAME7} 

- runFlow: 
    when: 
      true: ${ROLENAME8} 
    commands:
      - assertVisible:
          text: ${output.employeesPage.roleTile}
          containsDescendants: 
            - ${ROLENAME8} 

- runFlow: 
    when: 
      true: ${ROLENAME9} 
    commands:
      - assertVisible:
          text: ${output.employeesPage.roleTile}
          containsDescendants: 
            - ${ROLENAME9} 

- runFlow: 
    when: 
      true: ${ROLENAME10} 
    commands:
      - assertVisible:
          text: ${output.employeesPage.roleTile}
          containsDescendants: 
            - ${ROLENAME10} 

# Role not visible

- runFlow: 
    when: 
      true: ${ROLENAME01} 
    commands:
      - assertNotVisible:
          text: ${output.employeesPage.roleTile}
          containsDescendants: 
            - ${ROLENAME01}

- runFlow: 
    when: 
      true: ${ROLENAME02} 
    commands:
      - assertNotVisible:
          text: ${output.employeesPage.roleTile}
          containsDescendants: 
            - ${ROLENAME02}

- runFlow: 
    when: 
      true: ${ROLENAME03} 
    commands:
      - assertNotVisible:
          text: ${output.employeesPage.roleTile}
          containsDescendants: 
            - ${ROLENAME03}

- runFlow: 
    when: 
      true: ${ROLENAME04} 
    commands:
      - assertNotVisible:
          text: ${output.employeesPage.roleTile}
          containsDescendants: 
            - ${ROLENAME04}

- runFlow: 
    when: 
      true: ${ROLENAME05} 
    commands:
      - assertNotVisible:
          text: ${output.employeesPage.roleTile}
          containsDescendants: 
            - ${ROLENAME05}

- runFlow: 
    when: 
      true: ${ROLENAME06} 
    commands:
      - assertNotVisible:
          text: ${output.employeesPage.roleTile}
          containsDescendants: 
            - ${ROLENAME06}

- runFlow: 
    when: 
      true: ${ROLENAME07} 
    commands:
      - assertNotVisible:
          text: ${output.employeesPage.roleTile}
          containsDescendants: 
            - ${ROLENAME07}

- runFlow: 
    when: 
      true: ${ROLENAME08} 
    commands:
      - assertNotVisible:
          text: ${output.employeesPage.roleTile}
          containsDescendants: 
            - ${ROLENAME08}

- runFlow: 
    when: 
      true: ${ROLENAME09} 
    commands:
      - assertNotVisible:
          text: ${output.employeesPage.roleTile}
          containsDescendants: 
            - ${ROLENAME09}

- runFlow: 
    when: 
      true: ${ROLENAME010} 
    commands:
      - assertNotVisible:
          text: ${output.employeesPage.roleTile}
          containsDescendants: 
            - ${ROLENAME010}