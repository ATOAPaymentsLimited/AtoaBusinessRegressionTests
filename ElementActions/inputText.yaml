appId: ${APP_ID}
---
- runFlow:
    when:
        true: ${ELEMENT}
    file: tapOn.yaml
    env:
        ELEMENT: ${ELEMENT}
- inputText: ${TEXT}

- runFlow:
    when:
        true: ${KEYBOARD == 'true'}
    commands:
        - hideKeyboard

