appId: ${output.env.APP_ID}
onFlowStart:
  - runFlow: Elements/runScript.yaml
---
- assertVisible:
    text: ${output.yourBusinessDetailsPage.businessName}
    containsDescendants: 
      - ${output.yourBusinessDetailsPage.limitedCompany}